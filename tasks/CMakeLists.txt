# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(imu_an_spatialTaskLib)
ADD_LIBRARY(${IMU_AN_SPATIAL_TASKLIB_NAME} SHARED 
    ${IMU_AN_SPATIAL_TASKLIB_SOURCES})
add_dependencies(${IMU_AN_SPATIAL_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${IMU_AN_SPATIAL_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${IMU_AN_SPATIAL_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${IMU_AN_SPATIAL_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${IMU_AN_SPATIAL_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${IMU_AN_SPATIAL_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${IMU_AN_SPATIAL_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${IMU_AN_SPATIAL_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${IMU_AN_SPATIAL_TASKLIB_HEADERS}
    DESTINATION include/orocos/imu_an_spatial)

